{{ if or .Values.serviceMonitor.ui.enabled .Values.promExporter.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    {{- include "zwave-js-ui.labels" . | nindent 4 }}
    {{- with .Values.serviceMonitor.labels }}
    {{ toYaml . | nindent 4 }}
    {{- end }}
  name: {{ include "zwave-js-ui.fullname" . }}
spec:
  endpoints:
  {{- if .Values.serviceMonitor.ui.enabled }}
  - interval: {{ .Values.serviceMonitor.ui.interval }}
    port: {{ .Values.ports.ui.name }}
    {{- with .Values.serviceMonitor.ui.endpointAdditions }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
  {{- if .Values.promExporter.enabled }}
  - interval: {{ .Values.serviceMonitor.promExporter.interval }}
    port: {{ .Values.ports.promExporter.name }}
    {{- with .Values.serviceMonitor.promExporter.endpointAdditions }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
  {{- with .Values.serviceMonitor.namespaceSelector }}
  namespaceSelector:
  {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "zwave-js-ui.selectorLabels" . | nindent 6 }}
{{- end }} 
