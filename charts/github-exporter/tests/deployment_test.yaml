suite: github-exporter deployment
templates:
  - deployment.yaml

tests:
  - it: configures the startup probe by default
    asserts:
      - equal:
          path: spec.template.spec.containers[0].startupProbe.httpGet.path
          value: /
      - equal:
          path: spec.template.spec.containers[0].startupProbe.httpGet.port
          value: http
      - equal:
          path: spec.template.spec.containers[0].startupProbe.timeoutSeconds
          value: 2
      - equal:
          path: spec.template.spec.containers[0].startupProbe.failureThreshold
          value: 60
      - equal:
          path: spec.template.spec.containers[0].startupProbe.periodSeconds
          value: 5

  - it: configures the readiness probe timeout
    set:
      probes.readiness.timeoutSeconds: 9
      probes.readiness.periodSeconds: 15
    asserts:
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.timeoutSeconds
          value: 9
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.periodSeconds
          value: 15

  - it: can disable the startup probe
    set:
      probes.startup.enabled: false
    asserts:
      - notExists:
          path: spec.template.spec.containers[0].startupProbe